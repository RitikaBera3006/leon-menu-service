# FastAPI framework
fastapi~=0.109
uvicorn[standard]~=0.27

# Pydantic for validation
pydantic~=2.5
pydantic-settings~=2.1

# Database — this service owns its own schema/migrations (see alembic/)
sqlalchemy[asyncio]~=2.0
asyncpg~=0.29
alembic~=1.13

# API docs
scalar-fastapi==1.7.0

# sentry
sentry_sdk
